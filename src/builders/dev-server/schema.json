{
  "$schema": "http://json-schema.org/schema",
  "properties": {
    "skyuxAssetsBaseUrl": {
      "type": "string",
      "description": "Base URL to set"
    },
    "skyuxHostUrl": {
      "type": "string",
      "description": "Host URL"
    },
    "skyuxLocalUrl": {
      "type": "string",
      "description": "Local URL"
    },
    "skyuxOpen": {
      "type": "string",
      "description": "Opens local local or host",
      "enum": [
        "local",
        "host"
      ]
    }
  }
}
